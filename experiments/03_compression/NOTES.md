# NOTES — 03_compression

Not yet run. Secondary goal G6 (FR-10). Uses `configs/algorithms/diloco_int8.yaml` within the
Phase B campaign (`configs/grids/phase_b.yaml`). Blocked on `compress.py`'s error-feedback
residual invariant having a passing unit test (methods/diloco.md §3 invariant 4).
